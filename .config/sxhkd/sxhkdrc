super + Return
				alacritty
super + b
				firefox
super + f
				ranger
super + r
				rofi -show run
super + i
				nitrogen --set-zoom-fill --random &
super + c
				alacritty -e /home/muoscar/.scripts/launch-matlab.sh
super + shift + ctrl + q
				shutdown -P now
# Media keys
XF86AudioRaiseVolume
				pactl set-sink-volume @DEFAULT_SINK@ +1%
XF86AudioLowerVolume
				pactl set-sink-volume @DEFAULT_SINK@ -1%
XF86AudioMute
				pactl set-sink-mute @DEFAULT_SINK@ toggle
XF86AudioPlay
#				playerctl play-pause
				~/.scripts/playerctl-custom.sh
XF86AudioNext
				playerctl next
XF86AudioPrev
				playerctl previous
